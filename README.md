## Тестовое задание для компании idem.agency 

Реализация приложения "Гросс Маркет" на React.

### Доступные скрипты

**`npm start`**

Открывает [http://localhost:3000](http://localhost:3000) для отображения проекта в браузере.

**`npm run build`**

Сборка проекта в папку `build`.